{
  "name": "node-acquia-cloud",
  "version": "0.0.0",
  "description": "node-acquia-cloud",
  "license": "MIT",
  "repository": "",
  "author": "",
  "keywords": [
    ""
  ],
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib",
    "lint": "tslint --format verbose 'src/**/*.ts'",
    "build": "npm run clean && npm run lint && echo Using TypeScript && tsc --version && tsc --pretty",
    "test": "npm run build && mocha --compilers ts:ts-node/register --recursive test/**/*-spec.ts",
    "watch": "npm run build -- --watch",
    "watch:test": "npm run test -- --watch"
  },
  "dependencies": {
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.1",
    "rimraf": "^2.5.4",
    "ts-node": "^1.2.2",
    "tslint": "3.14.0-dev.1",
    "typescript": "2.0.0"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
